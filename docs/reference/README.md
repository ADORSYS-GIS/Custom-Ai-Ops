# Reference

**Information-oriented.** Dry, exhaustive, structured for lookup — not for reading start to finish ([Diátaxis: reference](https://diataxis.fr/reference/)).

Belongs here:

- [`glossary.md`](glossary.md) — the shared vocabulary (start here).
- Configuration flag tables for the engines we run, with our chosen values and rationale links.
- Model menu: which models we serve, at which precision, on which hardware.
- Index of benchmark results (the reports themselves live in `../benchmarks/`).
- Endpoint/API references for internal services.
